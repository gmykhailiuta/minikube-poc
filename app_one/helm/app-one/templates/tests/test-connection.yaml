apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-one.fullname" . }}-test-connection"
  labels:
    {{- include "app-one.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-one.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
